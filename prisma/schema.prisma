// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Example model (adjust to your schema)
model User {
  id    Int     @id @default(autoincrement())
  uid   String  @unique
  // ... other fields
}